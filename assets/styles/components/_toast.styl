.toast
  position fixed
  top $height_header
  left 50%
  width 100%
  max-width $width_max
  transform translateX(-50%)
  background-color rgba($bgColor_toast, 0.6)
  text-align center
  font-size $fontSize_toast
  padding 6px 15px 5px
  pointer-events none
  opacity 0
  z-index 1